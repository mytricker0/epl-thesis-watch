# This directory stores:
# - session.json   (your UCLouvain cookies)
# - watcher.log    (activity log)
# - STOP           (create this file to stop spam notifications)
#
# This directory is mounted as a Docker volume so data persists
# between container restarts.
